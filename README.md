CloudQA Automation Tests ( This project contains automated UI tests for the CloudQA Automation Practice Form. It is built using C#, NUnit, and Selenium WebDriver.) 

📋 Prerequisites \
Before running the tests, ensure you have the following installed on your machine:

Google Chrome: The tests are configured to run on the Chrome browser.

.NET SDK: (Version 10.0).

🚀 How to Run the Project
   Follow these steps to build and execute the automation suite.

1. Build the Project

   Compile the code to ensure all dependencies are resolved and there are no syntax errors.

       dotnet build
   
2. Run the Tests \
   Execute the NUnit tests. This will launch the Chrome browser and perform the automated actions. \
   
       dotnet test			
